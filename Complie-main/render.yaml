services:
  - type: web
    name: compile-server
    env: node
    buildCommand: cd server && npm install
    startCommand: node server/index.js
    envVars:
      - key: PORT
        value: 10000
