services:
  - type: web
    name: compile-server
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: PORT
        value: 5000
      - key: FRONTEND_URL
        sync: false
